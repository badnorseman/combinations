{
  "name": "combinations",
  "version": "1.0.0",
  "description": "Calculate combinations of n elements",
  "main": "index.js",
  "author": "Dan Jensen",
  "license": "MIT",
  "devDependencies": {
    "jest": "^22.0.4",
    "pre-commit": "^1.2.2"
  },
  "scripts": {
    "prettier": "prettier \"*/**/*.js\" --ignore-path ./.prettierignore --write && git add . && git status",
    "test": "jest"
  },
  "pre-commit": [
    "prettier"
  ]
}
